# Uncomment the next line to define a global platform for your project
# platform :ios, '11.3'

target 'fmdb_start' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for fmdb_start
  pod 'PopupDialog', '~> 0.8'
  pod "MMPopLabel"

  target 'fmdb_startTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'fmdb_startUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
